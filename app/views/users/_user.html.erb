<li>
  <%= gravatar_for user, size: Settings.index.img_size %>
  <%= link_to user.name, user %>
  <% if current_user.admin? && !current_user?(user) %>
    | <%= link_to t(".linkto_delete"), user, method: :delete,
        data: { confirm: t(".confirm_delete") } %>
  <% end %>
</li>
